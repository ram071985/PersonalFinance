name: Deploy Web

on:
  push:
    branches:
      - main
    paths:
      - 'PersonalFinance.Web/**'
      - 'PersonalFinance.Core/**'
      - '.github/workflows/deploy-web.yml'

env:
  AZURE_WEBAPP_NAME: personalfinance-web          # ← change if your app name is different
  DOTNET_VERSION: '8.0.x'
  PROJECT_PATH: './PersonalFinance.Web/PersonalFinance.Web.csproj'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore ${{ env.PROJECT_PATH }}

      - name: Build
        run: dotnet build ${{ env.PROJECT_PATH }} --configuration Release --no-restore

      - name: Publish
        run: dotnet publish ${{ env.PROJECT_PATH }} -c Release -o ./publish --no-build

      - name: Deploy to Azure
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_WEB }}
          package: ./publish
